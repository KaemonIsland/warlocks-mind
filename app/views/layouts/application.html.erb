<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %> </title>

    <%= render 'layouts/rails_default' %>
    <%= render 'layouts/shim' %>
  </head>

  <body>
    <h1>Hello Kaemon!</h1>
      <%= yield %>
      
    <%= debug(params) if Rails.env.development? %>
  </body>
</html>
