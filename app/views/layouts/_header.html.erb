<nav class="navbar navbar-expand-md fixed-top navbar-dark bg-dark">
  <%= link_to "Warlock Mind", root_path, class: "navbar-brand" %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if current_user.class == User %>
        <li class="nav-item">
          <%= link_to "Users", '#', class: "nav-link" %>
        </li>

        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Account
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to "Edit Profile", edit_user_registration_path, class: "dropdown-item" %>
            <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Items
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to "Weapons", user_weapons_path(current_user), class: 'dropdown-item' %>
            <%= link_to "Adventure Gear", user_gears_path(current_user), class: 'dropdown-item' %>
            <%= link_to "Armor", user_armors_path(current_user), class: 'dropdown-item' %>
            <%= link_to "Properties", user_properties_path(current_user), class: 'dropdown-item' %>
            <%= link_to "Damage Types", user_damage_types_path(current_user), class: 'dropdown-item' %>
            <%= link_to "Tools", tools_path, class: 'dropdown-item' %>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Log In", new_user_session_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
